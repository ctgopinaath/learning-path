import React from "react";
import ReactDOM from "react-dom/client";
import CloudComputingLearningPath from "./CloudComputingLearningPath.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <div style={{ maxWidth: 1100, margin: "0 auto", padding: "24px", height: "calc(100vh - 48px)" }}>
      <CloudComputingLearningPath />
    </div>
  </React.StrictMode>
);
